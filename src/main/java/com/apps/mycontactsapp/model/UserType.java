package com.apps.mycontactsapp.model;

/**
 * Enum representing the different types of users in the system.
 */
public enum UserType {
    /**
     * Represents a user with standard, restricted access.
     */
    FREE,

    /**
     * Represents a user with extended privileges.
     */
    PREMIUM
}
